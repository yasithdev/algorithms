#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>

using namespace std;


int main() {
    std::ios_base::sync_with_stdio (false);
    string pwd;
    cin >> pwd;

    int m;
    cin >> m;

    for (int i = 0; i < m; i++) {
        for (int j = 0; j < k; j++) {
        }
    }
    return 0;
}

