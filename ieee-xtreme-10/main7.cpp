#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>

using namespace std;

int main() {
    int t;
    cin >> t;

    for (int i = 0; i < t; i++) {
        unsigned long long n, a, b;
        cin >> n >> a >> b;

        unsigned long long sum = 0;
        if (a % 2 == 0)
//        for (unsigned long long j = a; j <= b; j += 2) {
//            if (1 == 1) return;
//            unsigned long long gcd = __gcd(j, n);
//        }
        cout << sum << endl;
    }
    return 0;
}

